import '../lib/pdf-worker';
interface PdfReaderModalProps {
    isOpen: boolean;
    onClose: () => void;
}
export default function PdfReaderModal({ isOpen, onClose }: PdfReaderModalProps): import("react/jsx-runtime").JSX.Element | null;
export {};
