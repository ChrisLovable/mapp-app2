interface AlarmModalProps {
    isOpen: boolean;
    onClose: () => void;
    currentText: string;
}
export default function AlarmModal({ isOpen, onClose, currentText }: AlarmModalProps): import("react/jsx-runtime").JSX.Element | null;
export {};
